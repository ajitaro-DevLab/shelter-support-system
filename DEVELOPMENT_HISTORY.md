# 開発履歴

この文書は、避難所業務支援Webアプリ V1.0が、どの課題を解くために各フェーズを追加したかを記録します。個々の判断理由は [DESIGN_DECISIONS.md](DESIGN_DECISIONS.md)、現在の完成範囲は [README.md](README.md)、時系列の一覧は [PROJECT_TIMELINE.md](PROJECT_TIMELINE.md) を参照してください。

## Phase 1：初期プロトタイプ

### 目的

紙運用と共存できる共通インターフェースの最小形を作り、ブラウザだけで避難所情報を確認できるか確かめることでした。

### 成果

- 出来事ベースのHome
- Leafletによる地図
- 5避難所のマーカーと簡易ポップアップ
- 避難所・物資データのJSON分離
- PC、タブレット、スマートフォンを意識した基本レイアウト

### なぜこの変更を行ったか

最初から入力や連携を広げると、現場で本当に必要な情報の範囲が見えにくくなります。まず「一覧できる」「地図で位置が分かる」「初期データを画面から分離できる」という土台に限定しました。

### 設計変更

業務別メニューではなく、「人が来た」「物が来た」「困りごとが出た」など、出来事を入口にする方針を採用しました。

## Phase 2：Home UI改善

### 目的

避難所の現場で、現在の施設と主要状態を迷わず確認できるHomeに整えることでした。

### 成果

- 固定の担当施設名表示
- 避難者数、水、食料、衛生、医療、更新時刻の要約
- 状態項目の簡易詳細表示
- 「本部へ伝える」を小さな送信操作へ整理
- 「今日のお知らせ」の簡素化と詳細ボタン

### なぜこの変更を行ったか

初期画面に操作を並べるだけでは、いま確認すべき状態が埋もれます。入力機能を増やさず、施設と状態を先に読める情報順序へ改めました。

### 設計変更

Homeを機能メニューではなく、選択中の避難所の状況を把握する画面として明確にしました。

## Phase 3：Information Board

### 目的

大型テレビ、プロジェクター、電子掲示板に、避難者が必要な生活支援情報を表示する専用画面を用意することでした。

### 成果

- `information.html` の独立表示
- 大きな文字と操作ボタンのない表示専用UI
- `notices.json` の読込
- 定期的な自動再読込

### なぜこの変更を行ったか

現場向け操作画面をそのまま掲示すると、不要な内部情報や操作部品まで見えてしまいます。読む人と目的が異なるため、公開表示を独立させました。

### 設計変更

後のV1.0通し試験で、固定の食事・医療・入浴欄を前提にせず、確定して公開された生活支援情報だけを表示する方針へ整理しました。

## Phase 4：HQ Dashboard

### 目的

本部が複数避難所の状況と、確認が必要な施設を一画面で把握できるようにすることでした。

### 成果

- 避難所一覧と統計
- 状態、避難者数、更新時刻、信頼度の表示
- 更新からの経過を示す情報鮮度
- Leaflet地図と簡易詳細

### なぜこの変更を行ったか

本部に必要なのは、全項目を細かく読むことより、どこを先に確認すべきか判断することです。状態と更新の古さを並べて見られる構成にしました。

### 設計変更

情報鮮度を独立した判断材料にし、状態が正常でも古い情報は確認対象になり得ることを表しました。

## Phase 4.5：HQ Dashboard操作性改善

### 目的

避難所が150施設規模になっても、確認対象を絞り込みやすい操作へ改善することでした。

### 成果

- 6項目の昇順・降順ソート
- 正常、注意以上、要支援、未更新、更新遅延の単一条件フィルター
- 人が読める経過時間表示
- 最終更新者、直近3件の履歴、連絡推奨理由の詳細表示

### なぜこの変更を行ったか

一覧が長くなると、固定順では古い情報や要支援施設を見落とします。入力機能は増やさず、優先順位を付けるための閲覧操作を追加しました。

### 設計変更

役割名の「担当者」ではなく、レコードの出所を示す「最終更新者」を採用しました。

## Phase 5：LauncherとEvent Center

### 目的

システム全体の入口と、今後のイベント入力を同じ型で追加できる基盤を作ることでした。

### 成果

- 4画面を別ウィンドウで開くLauncher
- 6つのイベント入口を持つEvent Center
- イベント説明、更新対象、反映画面を示す詳細パネル
- `events.json` によるイベント定義の分離

### なぜこの変更を行ったか

画面URLを個別に覚える負担をなくし、入力画面を大量に作る前に「何が起きたか」を選ぶ共通入口を確立するためです。

### 設計変更

医療、配食、詳細物流はイベント対象から外し、専門システムとの連携候補として扱う境界を明確にしました。

## Phase 6：避難者来所

### 目的

最初の実イベントとして、来所による人数変化をEvent CenterからHomeとHQ Dashboardまで一連で反映することでした。

### 成果

- ＋1、＋5、＋10、－1、－5、－10、整数直接入力
- 現在避難者数、更新時刻、最終更新者の更新
- localStorageによる更新差分の保持
- EventStoreによる共通保存処理
- Home、HQ Dashboard、履歴への反映

### なぜこの変更を行ったか

画面だけでなく、ひとつの出来事が保存と複数画面へつながることを、最小の入力項目で検証する必要があったためです。

### 設計変更

静的JSONは初期値のまま保ち、ブラウザ内のlocalStorageへ差分を保存する方式を採用しました。

## Phase 7：避難者数定時確認

### 目的

リアルタイム人数管理ではなく、「最後に確認された人数」と「確認時刻」を本部が判断できるようにすることでした。

### 成果

- 09:00、13:00、18:00の確認枠判定
- 変更なし、増減、実数訂正による短時間の正式確認
- 正式確認人数、時刻、確認枠、最終更新者の保存
- HomeとHQ Dashboardへの正式確認表示
- 確認未実施の判定
- 避難者数の情報鮮度を最終正式確認時刻基準へ変更

### なぜこの変更を行ったか

一時外出を人数減として扱わず、毎回の点呼も前提にせず、本部が信頼できる基準時点を持つためです。

### 設計変更

現在人数と正式確認人数を分離し、日中の増減と定時の確認済みスナップショットを別の意味として保存しました。

## V1.0完成前：通し試験のための最小実装

### 目的

朝から夕方までの避難所業務を、ひとつのブラウザ内で連続して再現し、画面間の反映と公開範囲を確認することでした。

### 成果

- 物資受領、避難所不具合、お知らせ更新の保存と反映
- 全イベントに共通する内部記録
- 画面は直近3件、内部は全件を保持する履歴方針
- 公開指定されたお知らせだけをInformation Boardへ反映
- テスト時刻指定とテストデータ初期化
- [V1_TEST_SCENARIO.md](V1_TEST_SCENARIO.md) による1日9イベントの通し試験

### なぜこの変更を行ったか

個別機能の動作確認だけでは、時刻の進行、正式確認、状態変化、公開情報の選別が連続したときの整合性を確認できません。V1.0の範囲を広げず、代表的な一日の流れに必要なイベントだけを追加しました。

### 設計変更

Information Boardを「予定の固定欄」から「公開を明示した確定情報の掲示」へ整理し、内部の不具合記録が自動公開されない境界を確定しました。

## V1.0到達点

V1.0は、単一ブラウザでの試作として、出来事の入力、ブラウザ内保存、現場表示、本部表示、公開表示までの流れを確認できる状態です。行政の正式運用や複数端末共有を完成させたものではありません。現状の限界は [KNOWN_LIMITATIONS.md](KNOWN_LIMITATIONS.md) を参照してください。

---

[READMEへ戻る](README.md) / [設計判断](DESIGN_DECISIONS.md) / [既知の制約](KNOWN_LIMITATIONS.md) / [タイムライン](PROJECT_TIMELINE.md)
