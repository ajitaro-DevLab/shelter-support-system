# V1実証版 AI作業ルール

このリポジトリでは、ユーザーが指定した `V1_DETAILED_SPECIFICATION_v1.2.md` を最上位の実装仕様として扱う。

仕様の優先順位は次のとおりとする。

1. `V1_DETAILED_SPECIFICATION_v1.2.md`
2. `V1_DEMONSTRATION_SPECIFICATION.md`
3. `CURRENT_SPECIFICATION.md`
4. `README.md`
5. その他の設計・履歴文書
6. AIの判断

## 現在のPhase

Phase 5 完了。V1実証版はCOMPLETE（2026-08-14）。次Phase（本格認証・オフライン対応等）はユーザーからの明示的な指示があるまで着手しない。

## 作業開始時

- 指定された仕様章を作業前に読む。
- Phase開始時、DB・API契約・複数画面に関係する変更、または仕様矛盾が疑われる場合は、関連する大章を再確認する。
- `git status --short` がcleanであることと、Phase開始checkpointが存在することを確認する。
- 1タスクを小さな変更単位に限定する。

## 禁止事項

- 仕様外の機能追加
- 指定範囲外ファイルの無断変更
- 未指示の大規模リファクタリング
- 依存パッケージの無断追加
- テストの期待値緩和、削除、skip
- 原因未確認の広範囲な修正
- インターネットへの直接公開
- 本格認証、オフライン同期、AI、OCR等の延期項目の実装

新規アイデアは原則として `IDEAS.md` のバックログ候補とし、現在のタスクから脱線して深掘りしない。

## 停止・報告規則

仕様が不明、矛盾、不足している場合は、当該部分を実装せず次の形式で報告する。

```text
SPEC_QUESTION:
- 該当仕様
- 問題
- 実装への影響
- 最小の選択肢
```

範囲外ファイルの変更が必要な場合は、実装せず次の形式で報告する。

```text
OUT_OF_SCOPE_CHANGE_REQUIRED

必要と考える変更:
対象ファイル:
理由:
変更しない場合の影響:
```

テスト仕様に誤りがあると判断した場合は、テストを変更せず `TEST_SPEC_CONFLICT` として報告する。

根本原因を確認できない場合は、推測で変更せず `ROOT_CAUSE_NOT_CONFIRMED` として報告する。

## タスク終了時

- 自動テストと既存回帰テストを実行する。
- `git status --short`、`git diff --stat`、`git diff` を確認する。
- 変更ファイル、追加ファイル、削除ファイル、仕様外変更、テスト結果、残課題を報告する。
- 受け入れ条件を満たしたタスクだけをtask-complete commitにする。
- 失敗を成功扱いせず、エラーを握りつぶさない。

先が見えた場合は別の選択肢をフラットに提示する。ただし、新規案は現在の実装へ混ぜずバックログとして分離する。
