# 設計判断

この文書は、避難所業務支援Webアプリ V1.0で「なぜその設計にしたか」だけを残すものです。実装の説明は [README.md](README.md)、変更の順序は [DEVELOPMENT_HISTORY.md](DEVELOPMENT_HISTORY.md) を参照してください。

## 1. 既存の災害管理システムを置き換えない

国や自治体の既存システムには、組織間共有や正式報告としての役割があります。本プロジェクトが同じ範囲を小さく作り直すと、二重管理と責任範囲の混乱を増やします。そのため、避難所現場の記録負担を減らす補助UIに役割を限定しました。

## 2. 業務別ではなくイベント駆動にする

災害時の避難所では、受付、物資、施設と役割を固定できるとは限りません。一人が複数の仕事を担う状況では、組織図から入力先を探すより、「人が来た」「物が届いた」「不具合が起きた」という出来事から入る方が判断が少なくなります。

## 3. 入力を最小限にする

記録の精密さを求めすぎると、忙しい時間帯ほど入力されなくなります。V1.0では、人数の定型増減、物資種別・数量・単位、状態の選択など、次の判断に必要な項目を優先しました。自由記述を増やさないのは、入力時間と表現のばらつきを抑えるためです。

## 4. 紙を否定しない

紙は、停電、端末不足、引き継ぎ、掲示などで使える強みがあります。すべてをデジタルへ移す前提にすると、障害時の代替手段を失います。本アプリは紙の記録と併存し、必要な情報共有を補助する立場を取ります。

## 5. 現在人数と正式確認人数を分ける

現在人数は、明確な来所・退所・訂正を反映した最新値です。正式確認人数は、指定した時点で確認済みとした基準値です。一時外出まで機械的に減算する運用を避け、本部が「最後に確認された人数」を別に判断できるよう、意味の異なる2つの値を分離しました。

## 6. 09:00、13:00、18:00を確認の節目にする

目的は連続監視や毎回の点呼ではありません。朝、日中、夕方という業務の節目に、変更なし・増減・実数訂正のいずれかで短時間に確認記録を残すためです。確認枠を設けることで、本部は数値を自動修正せず、確認が行われたかだけを判別できます。

## 7. Information Boardには確定・公開情報だけを出す

食事、医療、入浴などの予定は変更される可能性があります。未確定情報を大型画面へ出すと、多くの避難者を誤った場所や時刻へ誘導しかねません。そのため、項目名で自動公開せず、内容を確認して「公開」と指定した生活支援情報だけを表示します。

## 8. 内部の不具合を自動公開しない

設備不具合は本部が対応を判断する内部情報であり、そのままの表現が避難者向け案内に適するとは限りません。公開が必要な場合は、影響と案内内容を整理して別のお知らせとして登録します。内部共有と対外案内を分けることで、未整理情報の拡散を防ぎます。

## 9. 「担当者」ではなく「最終更新者」と呼ぶ

「担当者」は固定された役割や責任者を連想させますが、避難所では役割が流動的です。必要なのは、その記録を最後に更新した主体を追えることです。そのため、責任の所在ではなくデータの更新元を表す「最終更新者」を採用しました。

## 10. 状態と情報鮮度を分ける

「正常」という状態でも、更新が古ければ現在も正常とは限りません。反対に、更新が新しくても要支援である場合があります。状態と経過時間を別軸で示し、本部が連絡の優先順位を判断できるようにしました。色だけでなく「30分前」などの文字も添えます。

## 11. 詳細原因より状態入力を優先する

初動時に必要なのは、完全な原因分析より、注意や至急対応が必要かを共有することです。V1.0はカテゴリと状態を先に記録し、詳細な設備診断や対応管理は扱いません。入力を遅らせず、専門判断との境界も保つためです。

## 12. 専門システムと競合しない

医療、配食、物流には、それぞれ固有の安全要件、単位、責任体制があります。避難所の共通画面へ詳細機能を抱え込むと、誤用と保守負担が増えます。本アプリは状況の入口と概要共有に留め、詳細は専門システムとの将来連携候補とします。

## 13. localStorageをV1.0で採用する

V1.0の目的は、サーバーを設計することではなく、ひとつの出来事が複数画面へ反映される流れを短期間で検証することです。ブラウザ内に保存できるlocalStorageなら、既存JSONを壊さず、差分保持と通し試験を実現できます。採用は試作範囲に適したためであり、本番運用に適するという判断ではありません。

## 14. クラウドを採用しない

クラウド化には、通信障害、認証、権限、個人情報、監査、運用責任、費用を同時に設計する必要があります。それらを曖昧なまま追加すると、画面の試作より大きな未解決問題を生みます。V1.0では現場UIと情報の流れの検証に集中し、クラウドは別フェーズの判断としました。

## 15. 初期JSONと更新差分を分ける

初期データをJSON、操作後の変化をlocalStorageへ分けると、元データを保ったまま繰り返し試験できます。また、どこまでが配布時の基準で、どこからが操作結果かを区別できます。正式な永続化方式が未決定の段階で、JSONを書換可能なデータベースのように扱わないためでもあります。

## 16. 画面履歴は3件、内部記録は全件保持する

画面に全履歴を並べると、直近の変化を探しにくくなります。一方、通し試験では一日の流れを後から検証する必要があります。そのため、画面は直近3件に絞り、ブラウザ内の共通イベント記録は全件を保持します。

## 17. 小規模避難所を基準から外さない

専任のIT要員や多くの端末を前提にすると、小規模避難所ほど利用できません。大きなボタン、平易な表現、少ない操作、色以外の表示を選び、規模や役割分担が限定された現場でも理解できることを優先しました。

## 18. V1.0では個人情報を持たない

個人名簿や医療情報を扱うと、必要な安全対策と運用責任が大きく変わります。まず集計値と状態共有の有用性を確認するため、V1.0では個人を識別する情報を対象外にしました。

---

[READMEへ戻る](README.md) / [開発履歴](DEVELOPMENT_HISTORY.md) / [既知の制約](KNOWN_LIMITATIONS.md) / [タイムライン](PROJECT_TIMELINE.md)
